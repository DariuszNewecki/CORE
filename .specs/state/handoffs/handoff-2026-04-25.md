# Session Handoff — CORE

**Handoff prepared:** 2026-04-24 (end of session 4)
**GitHub state verified:** 2026-04-24 via `gh` CLI
**For:** Next CORE session, Claude.ai instance

---

## 1. Role & Interaction Contract

**ROLE**
Default to principal architect. Switch lenses when asked: compiler engineer for AST/audit logic, control-systems engineer for convergence arguments, GxP auditor for traceability claims, adversarial reviewer when asked to break rather than build, technical editor for Dev.to / docs drafts.

**INTERACTION CONTRACT**
- Verify before proposing. If the answer depends on code in `context_core.txt`, read it with view/grep before reasoning. Do not infer from memory when the file is available.
- Produce complete corrected files, not diffs or partial snippets.
- Produce exact Claude Code prompts, not multi-step manual instructions I have to translate.
- One focused question per turn. If I bundle, ask me to pick.
- `.intent/` files are read-only to Claude Code — any `.intent/` change must come back as a file for me to apply directly.
- State assumptions inline rather than asking clarifying questions I can answer myself from context.
- When uncertain, say so explicitly. Do not hedge-as-filler.

**DELIVERABLE DISCIPLINE**
- If I name a deliverable shape (Claude Code prompt, ADR, complete file, article), match it exactly. Do not substitute a different shape because it seems more thorough.
- Prefer a short correct answer over a long comprehensive one.
- No multi-step instructions unless I explicitly ask for a procedure.

**WHEN I PUSH BACK**
Treat pushback as signal, not conflict. If I say "you jumped to a conclusion" or "you didn't read the file first," acknowledge briefly and recalibrate — do not apologize at length.

**WHAT I AM NOT**
I am not a programmer. I am the architect/governor. Handle the code, don't hand me instructions to execute.

**VERIFICATION CHANNEL**
Claude.ai does not have direct GitHub access. For any claim about repo state (labels, milestones, issues, releases), produce a `gh` CLI prompt for me to run in Claude Code and rebuild your reasoning on the returned output. Do not cite state from memory or prior session transcripts as fact.

---

## 2. Current State (verified 2026-04-24)

**Band A: closed today.**

**GitHub state, verified via `gh` CLI:**

- **Labels: 25.** Full governed catalog in place. Covers types (`type:task`, `type:feature`, `type:adr`, `type:investigation`, `type:question`, `type:docs`, `type:cli`, `type:tooling`, `type:ci`, `type:epic`, `type:vision`), priorities (`priority:high`, `priority:medium`), statuses (`status:blocked`, `status:parked`, `status:verification-pending`), and cross-cutting (`governance-debt`, `hazard`, `false-positive`, `audit`, `organizational`, `roadmap`, `dependencies`, `github_actions`, `python`).
- **Milestones: 5 bands (A–E) plus 7 legacy release milestones.** Band A state CLOSED, Bands B–E state OPEN. All five bands are empty (0 issues attached). They exist as containers; Population C migration populates them.
- **Open issues: 1 (issue #84).** Title: "Reset Issue Tracker for Modern CORE Architecture (A0–A2 Era)." Opened 2025-12-07, labeled `type:feature`, no milestone assigned. Predates today's migration plan. Either this is the reset anchor and should now be closed, or it's orphaned and belongs under a band — next session's first judgment call.
- **Latest release: v2.2.2** (2026-02-28, "Self-Compliance & Hygiene Edition"). v2.3.0 not cut. Band A closure is the canonical trigger; governor discretion on timing.
- **Dependabot alerts: 21** (6 high, 10 medium, 5 low). Separate sweep, not today's scope.

**SESSION-PROTOCOL.md is live** at `.specs/planning/SESSION-PROTOCOL.md`. Session-open (§3) and session-close (§5) procedures apply going forward; the §6 issue template governs all new issue creation.

**Pending governor action (not blocking the migration session):**
- **A3 plan maintenance** (Step 3 of session-close). Expected edits: ADR-011 authoring row moves from implicit-pending to "Architectural Decisions Made"; attribution-principle blocker moves to "Resolved Blockers"; Milestone Summary for Phase 3 advances.
- **Release tag** (Step 4 of session-close). Target `v2.3.0` or similar. Can cut before or after the migration session.

**Housekeeping, non-blocking:**
- Milestone **v1.1: Operational Hardening** is state:open but has 0 open / 6 closed issues. Empirically complete. Close when convenient.
- README.md still advertises "Current Release: v2.2.0" — drifted from actual v2.2.2 tag.

---

## 3. Session Goal (one sentence)

Migrate the 13 items surfaced during the 2026-04-24 session 4 into GitHub issues, using `SESSION-PROTOCOL.md` §6 template. This is **Population C** from the prior session — today's items only, not older parked lists.

---

## 4. Source Material

Primary source: the 13 draft issue specs authored during the 2026-04-24 session 4 close. Titles and bodies exist as text in that session's transcript; they are not yet GitHub issues.

> **Governor to decide:** paste the session 4 transcript section containing the 13 drafts into the next session, or extract them to a dedicated file on disk (e.g., `.specs/planning/2026-04-24-population-c.md`) and point the next session at that path.

The 13 items in summary (labels and target milestones as drafted):

| # | Title | Labels | Milestone |
|---|-------|--------|-----------|
| 1 | Daemon-collision policy decision | `type:question`, `hazard`, `priority:high` | none |
| 2 | Dead-code investigation — ProposalService.mark_failed callers | `type:investigation`, `governance-debt`, `priority:medium` | Band D |
| 3 | Preemptive sweep for prose-triggering-rule false positives | `type:investigation`, `false-positive`, `priority:medium` | Band A |
| 4 | Wire `remediate.cognitive_role` into `auto_remediation.yaml` | `type:task`, `status:parked`, `priority:medium` | Band E |
| 5 | ADR candidate — "Describe rules; don't quote forbidden syntax" | `type:adr`, `priority:medium` | none |
| 6 | Runtime verification — 59ff25be on abandoned/indeterminate paths | `status:verification-pending` | Band A |
| 7 | Runtime verification — refactor 591acfdb | `status:verification-pending` | Band A |
| 8 | 15→17+ stuck `audit.remediation.failed` findings investigation | `type:investigation`, `governance-debt`, `priority:medium` | Band C |
| 9 | 28,287 NULL `resolved_at` rows — backfill or archive | `type:investigation`, `governance-debt`, `priority:medium` | Band C |
| 10 | `ProposalConsumerWorker.run()` exception branch bypasses `mark_failed` | `type:task`, `status:parked`, `priority:medium` | Band D |
| 11 | §7a revival path end-to-end exercise | `status:verification-pending` | Band A |
| 12 | Commit attribution sweep diagnostic artifact | `type:task`, `priority:medium` | none |
| 13 | Dependabot vulnerabilities — separate sweep | `type:task`, `priority:high` | none |

Scope note from the evening session: general meta-hazards (self-referential / protocol-level) were deliberately excluded from today's migration. Status of that exclusion — permanent or parked-for-later — is unresolved.

---

## 5. Expected Deliverable Shape

For each of the 13 items:
1. An issue spec matching `SESSION-PROTOCOL.md` §6 template
2. A `gh issue create` Claude Code prompt that produces the issue on GitHub

Batch once the pattern is proven on the first item. Start with issue #84 (the Dec 7 orphan) — decide close-vs-reassign before adding Band A's first new issue, so the band's population history is clean.

---

## 6. State-Scan Command for Session Open

Per `SESSION-PROTOCOL.md` §3. First move of the new session — verify current state before picking a lead:

```
Query the GitHub repo's current state and return raw output:

1. Open issues: `gh issue list --state open --limit 50 --json number,title,state,labels,milestone,createdAt`
2. Band milestones with counts: `gh api repos/DariuszNewecki/CORE/milestones?state=all --jq '.[] | select(.title | startswith("Band")) | {title, state, open_issues, closed_issues}'`
3. Latest release: `gh release list --limit 3`

Return raw output, no commentary.
```
