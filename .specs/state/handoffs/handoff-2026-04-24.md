<!-- path: .specs/planning/handoff-2026-04-24.md -->
---
kind: handoff
date: 2026-04-24
session_focus: Four sessions, same day. Morning closed Option A with ADR-009 (no fix; IntentGuard CLI-depth recurrence unrecoverable). Afternoon shipped §7+§7a Finding↔Proposal contract with ADR-010 (commit 62a84ff7). Evening closed resolved_at regression (59ff25be) introduced by 62a84ff7 and worker_uuid NULL via architectural refactor moving test.run_required posting to ProposalConsumerWorker. Session 4 closed Band A (ADR-011 attribution) end-to-end — principle authored, empirical sweep, two refactors, blocking-enforcement rule active, one self-referential false positive caught by the new rule and fixed same session. First full-arc principle closure (rules clear → enforcement real → code aligned) in one session since CORE's inception.
session_length: four sessions, same calendar day
a3_phase: 3
audit_state_at_close:
  verdict: PASS
  total_findings: 31
daemon_state: active
daemon_last_activity: 2026-04-24 13:18:10 CEST (post-refactor restart; no restart this session — both evening fixes still in-process, runtime verification of evening fixes pending natural proposal execution)
active_workers: 15
current_blocker: "None."
next_step: Pick one item from the parked list at session start, based on system state at that moment. No pre-commitment. Band A is complete; parked list grew.
---

# Session Handoff — 2026-04-24

A3 phase: 3 (Capability gaps). Four sessions today, same calendar day, single handoff per daily-cadence rule. Morning session closed Option A on live evidence (ADR-009). Afternoon session opened Option B as "wire revival," reframed on code+paper read to the full §7+§7a Finding↔Proposal contract (ADR-010), shipped three coordinated source files, and verified the new contract end-to-end on the first cycle post-restart. Evening session ran a state scan, closed the `resolved_at` regression silently introduced by 62a84ff7, and closed the `worker_uuid` NULL architectural violation via a larger-than-anticipated refactor. Session 4 took the evening session's close-of-session ADR candidate ("Workers own blackboard attribution; services do not post") and drove it through the full north-star arc in one sitting: ADR-011 authored, empirical sweep identifying two live violations, both refactored, blocking-enforcement rule authored and activated, one self-referential false positive caught and fixed same session. Audit PASS 31 throughout, with one transient FAIL during rule-activation closed within a single commit.

Last session: 2026-04-22 evening — Option B (engine-contract widening), Option A+ (resolved_at symmetric hygiene), Decision 1 (fix.placeholders safe-reclassification), ADR-008 parking impact_level externalization.

## What this session did — morning

Two documents plus one reusable procedure. No source changes.

| Commit / Doc | What | Verified by |
|--|--|--|
| [pending] | `.specs/decisions/ADR-009-cli-depth-block-passive-instrumentation.md`. Records the stand-down on the CLI-depth scope-narrowing fix; decision to rely on passive instrumentation (structured-violation persistence landed in `cf2ea63c` on 2026-04-23) rather than active probing or speculative hardening. Names the observability gap (no runtime signal for IntentGuard state integrity) and parks the continuous-signal design as a separate decision, adjacent to Option C from 2026-04-22. | Grounded in three diagnostic passes, all artifacts committed under `reports/diagnostics/`. |
| [pending] | `reports/diagnostics/2026-04-23_intentguard_cli_trace.md` + `reports/diagnostics/2026-04-24_intentguard_live_dump.md` + `reports/diagnostics/_ig_live_payload.py`. First is the out-of-process trace; second is the in-daemon live dump via `gdb` injection of the third. All three kept together — the payload is reusable against future occurrences. | Artifacts speak for themselves. |

### The investigation — Option A was a false lead

2026-04-22's handoff named this session's lead as "Option A: IntentGuard CLI-depth rule scope narrowing" on the theory that `cli.resource_first` (scope declared as `src/cli/resources/**/*.py`) was somehow firing against `src/will/workers/violation_remediator.py` during the `fix.placeholders` churn. The proposed fix was a YAML scope narrowing in `.intent/enforcement/mappings/`.

Three diagnostic passes ruled this out. **On-disk state:** `cli.resource_first` is defined once per registry, with the correct scope; the error string appears nowhere under `src/`; the pre-`cf2ea63c` `action_results` rows persist only the flat `error_message` string, so the `PolicyRule` that actually matched is not recoverable from the database. **Out-of-process load:** a freshly-instantiated IntentGuard against `/opt/dev/CORE` reports `is_valid=True` for the target under both `impact="write-code"` and `impact=None`. Every `cli.*` rule returns `Path.match=False` against the target. **In-daemon live dump** (the critical one): one IntentGuard singleton, 140 rules loaded, all `cli.*` scopes intact, `check_transaction` clean, two FileHandler instances both correctly pointing at the singleton. No rule mutation. No instance duplication. No scope drift.

The timestamps settled it. Failure window was 2026-04-23 14:34:58 – 16:46:05 UTC (nine failures, ~10-minute cadence). Self-recovery at 16:55:34 UTC (proposal `63d47846…`, commit `15c8ae3e`) against the same file with the same action. Current daemon started at 19:05 UTC, ~2 hours after the self-recovery. The process that produced the failures no longer exists. In-memory state at failure time is unrecoverable from any angle.

The conclusion the evidence forces: the failure was real, transient, self-recovered without a code change, and its cause is not recoverable from the current system. Narrowing the YAML scope would either have had no effect (if the cause is not scope-related) or would have masked a structural bug behind a scope override. Either outcome breaks the enforcement-real layer. ADR-009 closes the lead honestly.

### The in-daemon introspection capability

Yesterday's "look inside the running daemon" was not a capability we had. It is now. The procedure:

1. `sudo apt install gdb` (one-time; standard dev tool).
2. Payload script under `reports/diagnostics/` (read-only, introspection only — checked into git).
3. Invocation: `sudo /opt/dev/CORE/.venv/bin/python -c "…gdb one-liner using -ex per command…"` that attaches to the daemon PID, calls `PyGILState_Ensure`, executes the payload, releases the GIL, detaches. Full procedure captured in the session transcript for reuse.

Notes and gotchas that bit during the session, kept here so the next instance doesn't repeat them:

- `pyrasite 2.0` is unreliable. It shells out to `gdb` without checking the return code; if `gdb` isn't installed or the attach fails, it prints "injected OK" anyway. Silent-fail.
- `pyrasite` prints `"WARNING: ptrace is disabled. Injection will not work."` on Ubuntu Yama systems regardless of `ptrace_scope` value (it reads scope at import time, not at inject time). This warning appears even on `--help`. Ignore it if using `sudo gdb` directly.
- `ptrace_scope=1` is the default on this kernel and **is not overridden by `CAP_SYS_PTRACE`** via sudo — the scope check runs before privilege evaluation. For direct `sudo gdb` attach, this is fine (gdb bypasses pyrasite's scope check). For `pyrasite` as root, it silently no-ops.
- The one-shot `sysctl kernel.yama.ptrace_scope=0 → pyrasite → sysctl=1` flap was used once in this session and is noted as a recoverable but undesirable posture change. It is not required for the `sudo gdb` direct-attach path.

Test it before needing it. The payload is at `reports/diagnostics/_ig_live_payload.py`; it writes a markdown report to `reports/diagnostics/2026-04-24_intentguard_live_dump.md`. Re-running is cheap.

## What this session did — afternoon

One ADR, three source files, one commit. Contract shipped, verified live on the first post-restart cycle.

| Commit / Doc | What | Verified by |
|--|--|--|
| [pending] | `.specs/decisions/ADR-010-finding-proposal-contract.md`. Records the mid-session reframe of Option B from "wire §7a revival" to "close the full §7+§7a contract in three coordinated layers" after code+paper read showed three stacked gaps: (1) wrong terminal status (`resolved` vs `deferred_to_proposal`), (2) no `proposal_id` in finding payload, (3) no revival logic. Each depends on the previous; revival-only would have been inert. State-manager placement for revival (not worker) chosen on call-site coverage: all three `mark_failed` sites converge there. | Grounded in empirical pre-check (zero fix.placeholders activity in current daemon) + code reads of `ViolationRemediatorWorker.run`, `ProposalStateManager.mark_failed`, `BlackboardService` payload-merge idiom. |
| `62a84ff7` | Three source files shipping the §7+§7a contract: `src/body/services/blackboard_service.py` (+`defer_entries_to_proposal`, +`revive_findings_for_failed_proposal`), `src/will/workers/violation_remediator.py` (happy-path transition to `_defer_to_proposal`; dedup-subsume stays `_resolve_entries`; counter rename `entries_resolved`→`entries_deferred` in the report payload), `src/will/autonomy/proposal_state_manager.py` (`mark_failed` gains post-commit revival call, non-blocking error handling). Commit subject is the autonomous-remediation message from `d99f5919` — the commit captures both the manual file apply and the first autonomous run that touched the same file. | `core-admin code audit` post-apply: PASS, 32 findings, unchanged from baseline. Daemon restart (new PID 929929) cleanly up. Log at 11:33:43 shows `ViolationRemediatorWorker: created proposal 'd99f5919-...' for action 'fix.placeholders' (1 findings, 1 entries deferred to proposal)` — the string `entries deferred` is the proof that the new code is loaded and §7 transition fires. Proposal `d99f5919` succeeded 4s later. |

### The reframe — Option B was not what it looked like

Handoff framed Option B as "wire §7a revival in ProposalConsumerWorker." Reading the code against the paper revealed that framing was incomplete. §7 of `CORE-Finding.md` specifies a terminal status `deferred_to_proposal` that the code never transitions to — the existing `_resolve_entries` call marks findings `resolved` immediately on proposal creation. §7 also requires the proposal's ID to be written into the finding's payload at that transition, which never happens. §7a's revival contract queries findings by `payload->>'proposal_id'` — a query with no possible match against findings that never carry that field.

Revival-only would have compiled, run, and no-op'd forever. The three gaps had to close together.

The empirical match was also sharper than revival-only could explain. The 285-row churn on `audit.violation::purity.no_todo_placeholders::src/will/workers/violation_remediator.py` (2026-04-21 to 2026-04-23) represented ~276 successful proposals whose `fix.placeholders` action returned `ok=true` but did not remove the violation the sensor was detecting. Under revival-only, those findings stay `resolved`; the sensor's dedup (`status NOT IN ('resolved')`) re-posts, and the churn continues. Under §7+§7a, they transition to `deferred_to_proposal` (still in the dedup's active set), the sensor skips, the churn stops. The full contract addresses both the failure-path revival and the success-path churn.

ADR-010 records the reframe and the three-layer shape.

### First happy-path cycle — the system ate its own dogfood

Post-restart, at 11:33:43, ViolationRemediatorWorker processed 8 open findings. One of them was a `purity.no_todo_placeholders` violation on `src/body/services/blackboard_service.py` — the very file we had just shipped — caused by the word `TODO` in a governance marker comment (`TODO(two-log):`) in the `violation_remediator.py` docstring. The contract routed it through its own new machinery: the finding deferred to proposal `d99f5919`, the action ran deterministically, `fix.placeholders` removed the `TODO(` prefix, the commit landed as `62a84ff7`.

This is the closest CORE has come to self-governance on a structural change. The machinery shipped in the same session it was exercised on.

### One pre-existing bug surfaced, not caused by this change

Post-execution, `ProposalExecutor._post_test_run_required` raised `NotNullViolationError: null value in column "worker_uuid" of relation "blackboard_entries"` when inserting a `test.run_required` finding for the modified `.py` file. The INSERT at ProposalExecutor's raw-SQL path omits `worker_uuid`, which is NOT NULL. The error is caught by the surrounding `try/except` as a warning and does not propagate — proposal still recorded as succeeded. This is a pre-existing bug in the raw-SQL INSERT, not in the three files shipped today. Named in the parked list below; not chased.

### One unverified concern, deliberately parked

Claude Code's natural-language summary of the commit diff claimed `resolved_at = now()` was removed from `resolve_entries` / `abandon_entries` / `mark_indeterminate`. My draft files did not touch those methods. If the live-file state on disk pre-commit differed from the `context_core.txt` export I read (stale context), the diff summary could be correct and I silently regressed the 2026-04-22 Option A+ hygiene work on methods I didn't intend to edit. Not verified this session. Named as a parked item; detectable via query for terminal-state rows lacking `resolved_at`. Lesson: when editing from a context export older than the last known edit to that file, request a fresh export or read from disk directly.

## What this session did — evening

One state scan, two leads closed, two commits. Both fixes are runtime-unverified because observation requires specific trigger conditions that have not yet occurred post-restart.

| Commit / Doc | What | Verified by |
|--|--|--|
| (state scan) | Ground-truth read covering daemon state, audit, 24 minutes of daemon log since afternoon session close, blackboard active non-terminal findings, terminal transitions in window, and proposals since window. Surfaced: audit PASS 31 (one below baseline, consistent with session-close `fix.placeholders` removing the `TODO` in `violation_remediator.py`); §7 code loaded and cycling (0 entries deferred both observed cycles — no qualifying findings present); §7a unexercised (no `mark_failed`, no revive, no `deferred_to_proposal` rows in window); `worker_uuid` NULL fired exactly once at 11:33:47 (the session-close instant); 15 stuck `audit.remediation.failed::*` findings in the non-terminal set (new observation, not on any prior parked list). | Scan outputs preserved in session transcript; no artifact commit. |
| `59ff25be` | `src/body/services/blackboard_service.py` — restores `resolved_at = now()` on four SQL UPDATEs stripped by 62a84ff7: `resolve_entries`, `abandon_entries`, `mark_indeterminate`, and `resolve_dry_run_entries_for_namespace` (the fourth surfaced by Claude Code's preflight, not on the original suspect list). Full layout inversion to pre-62a84ff7 multi-line SET form — the four blocks are byte-identical to their pre-commit state. | `git diff --stat`: 1 file, +10 −3. `git diff` inspection: every added line part of a multi-line SET including `resolved_at = now(),`; every removed line a one-line SET. `core-admin code audit`: PASS, 31 findings. Pre-commit hooks (yaml, eof, whitespace, ruff, ruff-format): all green. |
| `591acfdb` | Refactor — move `test.run_required` finding-posting out of `ProposalExecutor` entirely into `ProposalConsumerWorker`. Two source files: `src/will/autonomy/proposal_executor.py` loses both INSERT blocks (single-path ~40 lines, batch-path ~40 lines) and the now-unused `text` and `json` imports; `execute()` and `execute_batch()` gain `changed_files` and `post_execution_sha` keys in their result dicts (with safe `[]` / `None` defaults hoisted to method scope so unconditional inclusion is scope-safe on dry-run, failed-action, and batch-iteration-failure paths). `src/will/workers/proposal_consumer_worker.py` gains a block in `run()` that iterates `result["changed_files"]` after each successful execution and calls `self.post_finding()` from the Worker base class — which fills `worker_uuid` and `phase` from the worker's identity, satisfying the blackboard_entries NOT NULL constraint that ProposalExecutor could not satisfy on its own. | `git diff --stat`: 2 files, ~−98 / +24 net. `python -c "import ast; ast.parse(...)"`: clean. `ruff check` both files: clean. `core-admin code audit`: PASS, 31 findings. Grep confirms: no `test.run_required` or `INSERT INTO core.blackboard_entries` remaining in `proposal_executor.py`; exactly one new posting block in `proposal_consumer_worker.py`. Daemon restarted at 13:18:10 CEST — 77 seconds after the refactor commit — so both evening commits are in-process. |

### The state scan — ground truth before a pick

The afternoon handoff's rule was "no pre-named lead; pick at session start, based on system state at that moment." Evening opened with a read-only scan that produced a compact ground-truth report over six numbered sections. The scan was uneventful in a useful way: daemon healthy, audit PASS 31, no proposal failures in the 24-minute window, no `worker_uuid` NULL recurrence beyond the single session-close instance. What it surfaced was the 15 stuck `audit.remediation.failed::*` findings that no prior session had named — added to the parked list, not chased.

Four candidates for the lead emerged: (1) the `resolved_at` hygiene verification (afternoon-parked, decaying signal window), (2) the 15 stuck findings (new, scope unknown), (3) the `worker_uuid` NULL fix (afternoon-parked, one concrete instance), (4) the `ProposalConsumerWorker.run()` exception-branch bypass (ADR-010 References, no evidence of firing). `resolved_at` chosen on a decay-of-evidence argument: the 211 fresh terminal rows in the post-refactor window were ideal for the regression query right now; every hour they accumulate dilutes the signal.

### Lead 1 — the resolved_at regression confirmed and closed

Three sections of evidence settled VERDICT B. `git show 62a84ff7 -- src/body/services/blackboard_service.py` revealed four coupled edits that the afternoon's natural-language diff summary had partially captured: the commit removes the `resolved_at = now(),` line from the SQL UPDATE **and** collapses the multi-line SET clause to a single line wherever only `status` and `updated_at` remained. The afternoon's concern was valid; the fix shape was not what the concern framed — the `resolved_at` removal was coupled to a layout change, not a standalone line drop. Pre-commit state had it; post-commit state did not; SQL backstop corroborated with 42 rows already written through the regressed path in the 60 minutes after 62a84ff7.

The fix path initially chosen was "minimal edit" (preserve the one-line layout, wedge `resolved_at = now(),` into it). Claude Code's preflight flagged this as creating a three-column one-line style that exists nowhere else in the file. Path 2 chosen instead: full inversion to pre-62a84ff7 byte-identical blocks. Commit `59ff25be` restored the four methods exactly. The daemon restart at 13:18:10 CEST brought the fix in-process.

A surprise: the preflight surfaced a fourth method on the same removal pattern (`resolve_dry_run_entries_for_namespace`) that was not on the afternoon's three-method suspect list. Inverted along with the three.

The 28,287 all-time `resolved` rows with NULL `resolved_at` are a separate, larger matter. They predate 62a84ff7 and are not introduced by this session; Option A+ closed some source paths but not all. Named in the parked list as a backfill-or-archive decision candidate for a future ADR.

### Lead 2 — the worker_uuid NULL was not what the handoff said it was

Afternoon's handoff described this as "field addition to the INSERT statement plus the caller's parameter map. Small scope." Verification against the afternoon's `context_core.txt` export surfaced a harder truth: `ProposalExecutor` is not a registered Worker, does not carry a `worker_uuid`, and neither the domain `Proposal` dataclass nor the `AutonomousProposal` ORM row carries one either. `CoreContext` is a services container, not a worker identity. The INSERT's missing field had no naive source.

Three candidate resolutions emerged: (A) thread `posting_worker_uuid` + `posting_worker_phase` through `ProposalExecutor.execute()` signature and all callers; (B) move the posting responsibility out of `ProposalExecutor` entirely into the calling `ProposalConsumerWorker`, which is a Worker with inherited `self.post_finding()`; (C) register a synthetic "proposal_executor" worker and give the executor its own UUID (rejected — ProposalExecutor is not a Worker in the constitutional sense).

Option (B) chosen explicitly as "the bigger refactor, done properly." The architectural justification is stronger than it initially appeared: the `worker_uuid` NOT NULL constraint encodes the constitutional principle that *every blackboard entry must be attributable to a registered Worker*. ProposalExecutor posting findings at all was a latent violation of that principle — the bug was the symptom. Moving the post into ProposalConsumerWorker doesn't just fix the bug; it closes a raw-SQL bypass that shouldn't have existed.

The refactor shipped clean. ProposalExecutor is now a pure executor: runs actions, returns a richer result dict with `changed_files` and `post_execution_sha`, no blackboard side effects. ProposalConsumerWorker iterates the result, calls `self.post_finding()` per `src/**/*.py` file changed, attribution flows through the Worker base class's `_post_entry` machinery which fills `worker_uuid` and `phase` correctly. The CLI execution path (`core-admin ... execute <proposal_id>`) degrades cleanly — human-invoked executions no longer post the finding, which is semantically correct: humans run their own tests. `execute_batch` has no production callers today but was updated symmetrically to prevent a latent bug if ever wired.

### Runtime verification is pending for both fixes

Neither fix is observable without a specific trigger. `resolved_at` hygiene: observable on the next terminal-state transition through any of the four methods producing a non-NULL `resolved_at`. Refactor: observable on the next APPROVED proposal that modifies at least one `.py` file under `src/` producing a `test.run_required::…` blackboard entry attributed to ProposalConsumerWorker's `worker_uuid`. As of session close, neither has occurred post-restart. The fixes are live on disk and in-process; first observation will come from normal operational activity.

### Two Claude.ai prompt errors, both caught by stop-and-report

Both leads this session produced initial prompts with structural errors that Claude Code flagged before committing. Worth keeping for institutional memory:

- `resolved_at` fix: initial prompt's verification gate assumed single-line additions (expected shape: "4 insertions, 0 deletions"). Actual fix required multi-line layout restoration (actual shape: 10 insertions, 3 deletions). Claude Code's preflight caught the premise mismatch and stopped. Path adjusted cleanly.
- Refactor — Option 1 scope bug: the initial spec placed `changed_files` and `post_execution_sha` unconditionally in both return dicts, but the variables were only bound on the `write=True` AND `all_ok=True` path. On dry-run, failed-action, or batch-iteration-failure paths, the unconditional references would UnboundLocalError at runtime. Claude Code's preflight traced the scope tree and stopped. Resolved by hoisting safe defaults (`[]`, `None`) to method-scope.
- Refactor — verification gate arithmetic slip: "3 occurrences per variable" contradicted "3 categories × 2 methods = 6," and was conceptually wrong regardless — a raw grep counts pre-existing uses that weren't new. Claude Code flagged the gate mismatch; structural checklist held; override issued.

Pattern: Claude.ai-side prompt-writing has a blind spot around *structural simulation*. Specs were written to a specific expected outcome without walking the code's actual control flow or counting pre-existing uses of changed names. The stop-and-report contract has been load-bearing — it has caught every scope and premise error this session. Worth internalizing: specs should state *structural* outcomes (e.g. "no INSERT remains in this file"), not literal *counts*, and should explicitly walk the scope tree before committing to unconditional-access patterns.

## What this session did — session 4

Five commits, one soft-reset history cleanup, one diagnostic artifact. Band A (attribution) closed end-to-end: principle authored, empirical sweep, two refactors, enforcement rule authored and activated, one self-referential false positive caught by the new rule and fixed same session. Audit PASS 31 throughout (one transient FAIL during rule-activation, closed in one commit).

| Commit / Doc | What | Verified by |
|--|--|--|
| (state scan) | Session-open ground-truth read over five sections: daemon liveness + audit, `59ff25be` verification (257/257 terminal-state rows carry `resolved_at` on the `resolved` path; `abandoned` and `indeterminate` paths unexercised), `591acfdb` refactor unverified (no APPROVED `.py`-touching proposal since restart), §7/§7a unexercised (no `failed` proposals since restart), 15 stuck `audit.remediation.failed::*` findings became 17 in the ~26-minute window since previous-session close (+2 drift). Scan confirmed no displacement of the evening-handoff's ADR-011 recommendation. Scan outputs preserved in session transcript; no artifact commit. | Read-only ground truth. |
| `35bde98a` | `.specs/decisions/ADR-011-worker-attribution-for-blackboard-entries.md`. Records the architectural principle the evening refactor (`591acfdb`) made operational but had not written down. Central cut: INSERT = attribution event (Worker-only); UPDATE = state transition on already-attributed rows (services permitted). Rejects three alternatives: threading `worker_uuid` through service signatures (propagates Worker concepts without closing the bypass); synthetic "proposal_executor" Worker (dilutes Worker concept); nullable `worker_uuid` (breaks consequence-log causality). Three open debts named: sweep for other bypass sites, enforcement rule authoring (now closed by `4ce1de85`), historical 28,287 NULL `resolved_at` backfill. | ADR-007 format match; principle statement block-bold per governor review; applied as-is. |
| `reports/diagnostics/2026-04-24_attribution_sweep.md` | Attribution sweep output. Three case-insensitive ripgreps across `src/`; classification over 4 distinct sites: 1 canonical Worker base class + 2 violations + 1 schema/model definition. Sweep-clean state means ADR-011 has a bounded, tractable violation surface. Initial sweep used a case-sensitive regex, caught by Claude Code's stop-and-report as missing the canonical Worker base-class site (which uses lowercase SQL). Re-run case-insensitive, arithmetic ties. Still untracked as of session close — candidate for a separate diagnostic-artifacts commit. | Claude Code's own stop-and-report on sweep methodology; verification gate `raw-hits = classified-sites` satisfied. |
| `8738595d` | Refactor 1 of 2. `src/body/atomic/remediate_cognitive_role.py` no longer performs raw-SQL INSERT; returns `finding_to_post` dict in `ActionResult.data`. `src/will/workers/proposal_consumer_worker.py` extended: new block in `run()` iterates `result["action_results"]` and posts any `finding_to_post` present via `self.post_finding()`. Generalization beyond the one-action case — **establishes `finding_to_post` as the canonical convention** for any atomic action that needs to cause a finding to be posted. Claude Code's second stop-and-report caught the original spec's naive access-path assumption (`result.data.get("finding_to_post")`) and the correct shape was `result["action_results"][aid]["data"]["finding_to_post"]` — spec revised, refactor landed correctly. | audit PASS 31. ruff clean. Zero INSERT hits in the atomic action file; exactly one new `self.post_finding()` call in the Worker file. |
| `35bde98a` + `794a4480` (after history cleanup, see below) | Refactor 2 of 2. `BlackboardService.revive_findings_for_failed_proposal` becomes UPDATE-only; returns revival outcome dict (`proposal_id`, `failure_reason`, `revived_count`, `revived_finding_ids`, `revived_subjects`). `ProposalStateManager.mark_failed` stops calling revive — eliminates the service-chain. `ProposalConsumerWorker.run()` orchestrates `mark_failed` (via executor) → revive → `self.post_report()` in the ok=False branch. `ProposalExecutor.execute()` and `execute_batch()` gain `failure_reason` in their ok=False result dicts so the Worker can persist it on the revival report with full fidelity. Paper-to-code alignment: **CORE-Finding.md §7a.3 explicitly assigns the report post to `ProposalConsumerWorker`; afternoon's implementation had it in `BlackboardService`**. Session 4 refactor closes the paper↔code gap without touching the paper. Three dormant/dead call sites named as known ADR-011-compliant-by-parking in the commit message. | audit PASS 31. Attribution sweep re-run across `src/`: exactly 1 hit (the canonical Worker base class). All four refactor files clean. |
| (history cleanup, no artifact commit) | The running daemon committed `fix.format` during the refactor-2 write window, sweeping our hand edits plus both `.specs/` files into commit `211c2dd2` titled `fix(0b359369): Autonomous remediation: fix.format...` — governance-surface refactor landed under an autonomous-format commit message, a consequence-log failure happening to CORE's own development. Local-only branch (6 commits ahead of origin, nothing pushed) made cleanup contained: soft-reset `HEAD~1`, selective restage, two honest commits (`35bde98a` ADR-011 doc, `794a4480` four refactor-2 source files absorbing the daemon's format cleanup with acknowledgement in the commit message). Handoff doc left unstaged for session-close. | `git show 211c2dd2 --stat --summary` confirmed separable scope; post-cleanup log order verified; pre-commit hooks re-passed; attribution sweep re-run = 1 hit, PASS 31. |
| `4ce1de85` | Band A step two — the enforcement rule. Two files: `.intent/rules/architecture/blackboard.json` (rule document) + `.intent/enforcement/mappings/architecture/blackboard.yaml` (engine mapping). `regex_gate` with single-file whitelist for `src/shared/workers/base.py`. Pattern: `(?i)INSERT\s+INTO\s+(?:core\.)?blackboard_entries`. Enforcement: `blocking`. Prose length (statement ~45 words, rationale ~50 words) matches comparable rules in the repo after governor push-back on initial 3×-too-long draft. | Rule fires on first audit run post-application. |
| `10bf08de` | Self-referential false positive fix. The function docstring in `src/body/atomic/remediate_cognitive_role.py` explaining ADR-011 compliance contained the literal phrase "every INSERT into core.blackboard_entries" — the rule (correctly) caught it. `regex_gate` is a deterministic string matcher and cannot distinguish SQL from prose. Rewording to "every blackboard write must flow through Worker attribution" resolved the audit `FAIL`. Writing convention established: **describe rules; don't quote their forbidden syntax**. | audit PASS 31. Rule-specific regex hit count in file: 0. Broader `INSERT` hit count: 0 (both prose sites reworded). |

### The sweep surfaced a dormant pipeline, not just violations

The attribution sweep identified 2 violations: `remediate_cognitive_role.py` (atomic action) and `blackboard_service.py:837` (§7a revival report). The first looked like dead code on shallow read — `auto_remediation.yaml` has no entry for `ai.cognitive_role.no_hardcoded_string → remediate.cognitive_role`, so the action is statically unreachable via the standard dispatch chain. The governor's push-back — *"that would suggest it got created at some point for some good reason... the question is: is designed functionality needed by any stretch of the imagination"* — forced a second look that revealed the full downstream architecture.

The action is one half of an architected two-Worker pipeline: `remediate_cognitive_role` (producer) → `prompt.artifact` findings → `CallSiteRewriter` (consumer, full Worker declaration at `src/body/workers/call_site_rewriter.py`) → PromptModel (LLM rewrite) → Crate (sandbox) → Canary validation → Apply via CrateCreationService → git commit. This is **Class B of CORE's remediation architecture** — LLM-semantic rewrites with safety gates, distinct from Class A (deterministic fixes like `fix.placeholders`, `fix.format`) handled directly by `ViolationRemediatorWorker`. `prompt.artifact` has exactly one producer and one consumer; they're a matched pair. Deletion would have amputated a working architecture. Refactor-while-dormant is the cheapest time to bring it into ADR-011 compliance — no runtime behaviour depends on current implementation.

Three options emerged: refactor only, refactor + wire `auto_remediation.yaml`, or park both. Option A chosen (refactor only; wiring remains a separate governor decision). Outcome: the `finding_to_post` convention now exists as canonical infrastructure for any future atomic action that needs to post findings.

### The daemon-collision hazard, demonstrated rather than theoretical

Mid-refactor-2, the running daemon's `fix.format` cycle caught ruff-format drift on `proposal_consumer_worker.py` (one of our in-flight files) and committed it autonomously at `211c2dd2`. The commit swept our hand edits — four source files, plus the ADR-011 document, plus the handoff doc — into a single commit titled `fix(0b359369): Autonomous remediation: fix.format (1 violation(s) — rules: workflow.ruff_format_check)`. A governance-surface refactor landed under an autonomous-format commit message: **a consequence-log failure the system is designed to prevent, happening to its own development**.

Cleanup was possible only because nothing was pushed. `git reset --soft HEAD~1`, selective restage, two replacement commits honestly titled (`35bde98a` for the ADR, `794a4480` for the refactor). The daemon's format change folded into `794a4480` with acknowledgement in the commit message — separating it hunk-by-hunk had low governance value, since the format change is idempotent on semantics.

Policy question surfaced, not resolved: **do we stop the daemon for band-A hand-refactor sessions, scope hand-refactors to files outside active remediation targets, or accept-and-clean each session?** Parked.

### Band A step two — the enforcement rule caught itself same session

After refactor 2 closed the sweep cleanly, step two — author the rule that would catch the next violation — was a single-session arc:

- Engine choice: `regex_gate`. Per-file, supports `scope.excludes` for single-file whitelisting, no new sensor code needed.
- Rule shape: two `.intent/` files following the codebase's existing `rule_document.schema.json` convention.
- Prose length: first draft ~3× longer than comparable rules; revised after governor push-back to match convention (~45-word statement, ~50-word rationale).
- Enforcement level: `blocking`. Pattern is deterministic, whitelist is a single file, sweep confirmed zero false positives pre-application.

Applied. Audit re-run → `FAIL`, one `ERROR`-severity finding at `remediate_cognitive_role.py:58`. Diagnosis: the function docstring explaining ADR-011 contained the literal pattern "INSERT INTO core.blackboard_entries" as part of describing the principle. `regex_gate` cannot distinguish SQL from prose; the rule did exactly what it was designed to do. Fix was a 6-line docstring reword. Commit `10bf08de`. Audit PASS 31.

This is the first time a CORE rule fired on its own session's documentation within minutes of being applied. **The system is self-governing on at least one principle now.** That is a milestone worth naming.

## What this session did NOT do

- Did not runtime-verify either evening fix. Named as parked. (Session 4 state scan confirmed `59ff25be` passively verified on the `resolved` path only; `abandoned`/`indeterminate` paths unexercised; refactor `591acfdb` unverified.)
- Did not investigate the 15→17 stuck `audit.remediation.failed::*` findings surfaced by the state scan. Named as parked. Session 4 state scan confirmed drift is still non-zero (+2 in 26 minutes).
- Did not backfill or archive the 28,287 all-time rows with NULL `resolved_at`. That debt predates 62a84ff7 and is separate work; candidate for ADR. Named as parked.
- Did not exercise §7a revival — no proposal has failed since afternoon's restart. Still unexercised as of session 4 close.
- Did not address the `ProposalConsumerWorker.run()` exception branch bypass of `mark_failed` (ADR-010 References, still parked).
- Did not revisit the 2026-04-22 longer parked list.
- Did not touch `.intent/` outside of the session-4 attribution rule files (`4ce1de85`).
- Did not run the attribution rule against the broader codebase beyond the files touched this session. A preemptive sweep (`rg -ni 'INSERT\s+INTO\s+(?:core\.)?blackboard_entries' src/ tests/ .specs/ .intent/`) for other prose sites containing the rule pattern has not been run.
- Did not begin Band B (the two-log consequence chain ADR).
- Did not commit `reports/diagnostics/2026-04-24_attribution_sweep.md`. Still untracked.
- Did not wire `ai.cognitive_role.no_hardcoded_string → remediate.cognitive_role` in `auto_remediation.yaml` — the atomic action is ADR-011-compliant but still dormant. Wiring is a separate governor decision.

## Carry-over — next session

**No pre-named lead.** The daily-cadence rule stands: each session's lead is chosen at session start, based on system state at that moment. The parked list is long enough that any choice is defensible; the discipline is to pick one, close it, and stop.

### Parked — available for selection

From session 4:

- **Daemon-collision policy decision.** Demonstrated today, not theoretical. Cleanup was possible only because the branch was local and nothing was pushed. Governor choice: stop the daemon for band-A hand-refactor sessions, scope hand-refactors to files outside active remediation targets, or accept-and-clean each session. Small decision; candidate ADR if the choice is "scope hand-refactors."
- **Dead-code investigation surfaced by refactor 2.** Three call sites of `mark_failed` are dead or dormant: `ProposalService.mark_failed` (dead, no callers), `ProposalService.execute_workflow` (dead, no callers), `ProposalExecutor.execute_batch` failure branch (dormant, no production callers). All three are ADR-011-compliant-by-parking per `794a4480`'s commit message. Investigation: orphaned work or waiting for a caller? One-session-sized.
- **Preemptive sweep for prose-triggering-rule false positives.** Beyond the docstring fixed today, other files may contain the literal pattern in comments, error messages, or log strings. Cheap to run now while the rule is fresh.
- **`remediate.cognitive_role` cold-path wiring.** Action is ADR-011-compliant but dormant — no `auto_remediation.yaml` entry. Activating wires up the Class B pipeline (LLM rewrite / Crate / Canary) for the `ai.cognitive_role.no_hardcoded_string` campaign. Governor-direct `.intent/` edit.
- **ADR candidate: "Describe rules; don't quote forbidden syntax."** Tiny writing-convention ADR codifying the discipline surfaced by today's self-referential false positive. One-page.
- **Diagnostic-artifacts commit.** `reports/diagnostics/2026-04-24_attribution_sweep.md` is still untracked. Low priority; can land with next diagnostic or on its own.

From evening:

- **Runtime verification of 59ff25be on abandoned/indeterminate paths.** The `resolved` path has 257 verified rows (session 4 state scan); `abandoned` and `indeterminate` paths have not been exercised post-restart. Passive observation only; no work required unless positive confirmation is wanted on the other two paths.
- **Runtime verification of refactor 591acfdb.** Observable on the next APPROVED proposal that modifies at least one `.py` file under `src/` producing a `test.run_required::…` blackboard entry with proper `worker_uuid` attribution. Still pending as of session 4 close.
- **15 stuck `audit.remediation.failed::*` findings** — now 17+, drift rate non-zero, un-inspected.
- **28,287 all-time rows with NULL `resolved_at`.** Historical governance debt. Backfill (`UPDATE … SET resolved_at = updated_at WHERE resolved_at IS NULL AND status IN …`) or archival (`*_archive` table) decision — each has different audit and consequence-log implications. Candidate for ADR.

From afternoon (still open):

- **`ProposalConsumerWorker.run()` exception branch bypasses `mark_failed`.** Named in ADR-010 References. When `executor.execute()` raises rather than returning `ok=False`, the proposal stays in `executing` forever and revival never fires.
- **§7a revival path end-to-end exercise.** No proposal has failed post-restart to exercise it.

From morning / earlier:

- **Option C from 2026-04-22 — cold-registry observability.** Same family of gap as ADR-009. Candidates for merged ADR.
- **Option D from 2026-04-22 — fix.modularity DRAFT 44441112 review** (11 SplitPlans). Read-only inspection first to size the review.
- **IntentGuard CLI-depth block recurrence.** Parked in ADR-009. If it fires again, `cf2ea63c` persistence captures rule attribution directly.

From 2026-04-22's longer parked list — ADR-007 Consequences addendum, 13-file class_too_large review ADR, ContextBuilder wiring for `build.tests`, path-mapping, `action_executor` guards, daemon composition root, dead `auto_remediation.yaml` entries, `proposals show` logger bug, `logic.di.no_global_session`, `autonomy.tracing.mandatory`, `purity.no_ast_duplication`, `ai.cognitive_role.no_hardcoded_string` campaign, `architecture.api.no_body_bypass` — all still parked.

### Band-level strategic work (from session 4's architect plan)

- **Band B — two-log consequence chain.** Strategic arc, GxP-load-bearing, ADR first then code. Biggest remaining feature in the architecture. Not started.
- **Band C — historical debt resolution.** Includes 28,287 NULL `resolved_at`, 17+ stuck findings, 107 unmapped rules classification.
- **Band D — engine integrity completion.** Remaining tactical items: ContextBuilder wiring, path mapping, action_executor guards, daemon composition root, exception-branch `mark_failed`, IntentGuard observability, rule campaigns.
- **Band E — outward-facing (post-D).** External-repo validation, `core-admin ask`, `impact_level` externalization, dormant `remediate.cognitive_role` campaign activation.

## Open questions — next session

1. **Will either evening fix produce a positive runtime signal under normal operation, or does verification need a synthesized trigger?** Session 4 confirmed passive verification on the `resolved` path (257/257); `abandoned`/`indeterminate`/refactor-`591acfdb` paths still pending.
2. **Are the 15→17+ stuck `audit.remediation.failed::*` findings one pattern or many?** Session 4's +2/26-minute drift rate is non-zero. If one, a single worker-path investigation closes them. If many, it's a longer audit-delivery shape.
3. **Is the 28,287-row historical `resolved_at` NULL pattern still accumulating after 59ff25be, or is the accumulation rate now zero?** A time-bucketed query answers cheaply.
4. **Does the daemon-collision hazard require a policy decision now, or do we accept-and-clean until it bites harder?** Today's cleanup pattern worked on local history; same situation post-push is political rather than technical.
5. **Will the attribution rule surface false positives in the broader codebase, or was today's docstring the only one?** Cheap to check proactively; cheap to wait.
6. **Surviving from afternoon:** Does the `entries_deferred` report-key change break any downstream consumer?
7. **Surviving from 2026-04-22:** verdict-threshold semantics, historical resolved count composition, moderate-risk second-axis, "Changes recorded: 0 files" coherence.

## Hazards worth naming

**Daemon-collision hazard — demonstrated, not theoretical, still unmitigated.** Today it was caught early, the branch was local, cleanup was tidy. The same situation post-push is political rather than technical. A band-A hand-refactor session that spans multiple hours has a non-zero probability of collision on any file the daemon's active remediation path touches. Mitigation options exist (see parked list); no policy chosen yet.

**Self-referential false-positive hazard — new this session.** Any deterministic string-matching rule (regex_gate and similar) is susceptible to catching its own documentation or ADR references that quote its forbidden pattern. Surfaced by session 4's `architecture.blackboard.worker_only_inserts` rule firing on the ADR-011 explanation in `remediate_cognitive_role.py`'s docstring. The correct response is writing discipline, not rule complexity. Convention established: describe rules; don't quote their forbidden syntax. Candidate for tiny ADR.

**Stale context-export hazard — still unresolved, now documented as the direct cause of a regression.** Afternoon's drafts of the §7+§7a source files came from a `context_core.txt` export. Between that export's timestamp and the live files on disk, the 2026-04-22 Option A+ hygiene work had edited methods the afternoon drafts didn't intend to touch. Afternoon's "preserve all other methods" approach silently regressed those edits — exactly the hazard named in the afternoon handoff. Evening found and fixed the regression (59ff25be), but the structural fix for the hazard itself was not applied. Structural fix: for any session that edits a file last-modified after the current context export, request a fresh export of that file (or read from disk directly) before drafting. Lighter-weight mitigation: after any multi-file commit, spot-check that unchanged methods' diffs are empty, not summarised. Claude.ai's prompt-writing should default to "read the file from disk first" whenever the target file has commits newer than the current export's timestamp.

**Claude.ai prompt-writing has a state-space-coverage blind spot.** Four prompts across sessions 3-4 had scope bugs that would have shipped broken code if applied — unconditional-dict-writes without checking variable binding on all paths, verification gates that assumed a specific diff shape without simulating the actual edit, case-sensitive regexes where the language being matched is case-insensitive, naive access paths that didn't walk the actual return-shape. Stop-and-report caught all four; that is not a substitute for writing better prompts. Specs should state structural outcomes, not literal counts, and should walk the scope tree before committing to unconditional-access patterns.

**Both evening fixes are runtime-unverified on most of their surface.** Disk state is correct and in-process since 13:18:10 CEST. Session 4 state scan confirmed `59ff25be` on the `resolved` path only (257 rows); `abandoned`/`indeterminate` and refactor-`591acfdb` still untested by natural operation. Expected gap, not a hazard in itself — but if something goes wrong with either fix, the first symptom may be subtle rather than loud.

**In-memory governance state is still not externally observable.** Morning's live-dump capability is a diagnostic tool requiring sudo and a running target process. No continuous signal. Parked in ADR-009.

**Pre-`cf2ea63c` evidence is still lost.** Nine failing rows from 2026-04-23 carry only the flat `error_message`.

**The cold-registry hazard from 2026-04-22 is still live.** Daemon reload semantics for action decorators remain unchanged — stale until restart, no signal, no warning.

**§7a revival path has not been exercised.** Code is live, test is pending a natural or synthesized failure.

**Prose sites containing the attribution-rule pattern beyond today's fix are unknown.** Reactive fix worked; a 30-second preemptive sweep across `src/ tests/ .specs/ .intent/` would size it.

## North-star ordering — updated

Rules clear → enforcement real → code.

- **Rules clear:** ADR-011 authored and accepted (session 4). First fully-documented attribution principle. ADRs 001–011 hold. Session 4 also closed a paper↔code gap for §7a revival (CORE-Finding.md §7a.3 vs. afternoon's `BlackboardService` implementation) via refactor, not paper change — **first principle to complete the principle → paper → code alignment cycle in-session**.
- **Enforcement real:** advanced four steps across the day. `resolved_at` hygiene restored on four terminal-state methods (`59ff25be`). `worker_uuid` architectural violation closed (`591acfdb`) — raw-SQL bypass eliminated, attribution flows through Worker identity. `remediate_cognitive_role` raw-SQL INSERT eliminated (`8738595d`); `finding_to_post` convention established. `blackboard_service` §7a revival-report INSERT eliminated (`794a4480`); Worker orchestrates. Attribution rule `architecture.blackboard.worker_only_inserts` active at `blocking` severity (`4ce1de85`) — **first ADR-011 enforcement site**. Rule caught itself on its own session (`10bf08de`).
- **Code:** seven commits across four sessions (`62a84ff7`, `59ff25be`, `591acfdb`, `35bde98a`, `8738595d`, `794a4480`, `4ce1de85`, `10bf08de`). One daemon-collision commit (`211c2dd2`) replaced via local soft-reset. Audit PASS, 31 findings, unchanged across the arc (with one transient FAIL during rule-activation, closed in one commit).

Today's net across four sessions: one paper↔code contract shipped and exercised, one regression introduced-and-fixed same day, one architectural bug closed via refactor, one full band (Band A, attribution) closed end-to-end, first self-governing-catch demonstrated. The parked list grew in three of the four sessions — the discipline to carry forward remains stopping when each session's named lead closes, rather than pulling each newly-exposed thread.

Band A closure is the first full-arc principle closure (rules clear → enforcement real → code aligned, all three advanced together) in one session since CORE's inception. That is not a common shape; it is a specific shape that requires the principle to be small enough and the machinery mature enough for all three layers to advance together. Band A had those properties; the remaining bands likely do not in equal measure (Band B's consequence-log scope makes it multi-session; Band D's engine integrity picks are individually one-session but do not collectively close any single principle).

Current blockers: none. Daemon: active, restarted at 13:18:10 CEST (evening's refactor), running uninterrupted through session 4. Audit: PASS, 31 findings, unchanged. Blackboard: awaiting first post-restart terminal transition on abandoned/indeterminate paths and first post-restart `src/**/*.py`-modifying proposal execution to produce runtime signals for the evening fixes. Active workers: 15. Next step: pick one parked item at session start.

---

*Session-level postmortem worth preserving — morning:* Option A was taken on faith from the 2026-04-22 handoff's framing ("the rule is almost certainly mis-scoped in `.intent/`"). That interpretation was made before live evidence was available. Producing the YAML fix without first verifying the live state would have shipped a confident false close. The lesson: when a handoff names the fix, the fix is the thing to verify hardest, not to accept. "Verify before proposing" applies to the handoff itself, not only to the code.

*Session-level postmortem worth preserving — afternoon:* The handoff framed Option B as a one-file revival wire-up. Code+paper read revealed a three-layer contract. The temptation to implement the narrow framing would have shipped inert code. The reverse lesson also applies: once the contract shipped and the first cycle verified, I kept pulling at newly-surfaced threads (stale-context concern, `worker_uuid` bug, verification-of-verification) instead of banking the win. The discipline to carry forward: one lead per session, stop when it closes, park everything else without inspecting it. The parked list is a feature, not a failure — it's how the system records what it saw without paying the cost of chasing it.

*Session-level postmortem worth preserving — evening:* The afternoon handoff scoped `worker_uuid` NULL as a three-line plumbing fix. Verification in Claude.ai's context tools showed that framing was architecturally wrong — not off by a line or two, but wrong about what the fix shape was at all. The right move was to stop, name the reframe (plumbing vs. refactor), and put the architectural decision back to the governor before producing any prompt. The handoff-as-scoping-artifact was wrong in a load-bearing way; verifying against the code corrected it. The broader lesson is consistent with morning's: handoffs encode the author's best-available understanding at write-time, not ground truth. Each session's verification against live code is the source of truth, and both morning and evening this session had to overturn the prior handoff's framing to close a lead honestly. That pattern itself is information — it means handoff-framing should describe what was observed with low-confidence interpretation explicitly flagged, not pass diagnostic conclusions forward as fact.

Claude.ai-side, the session exposed a blind spot around state-space simulation in prompt writing. Two prompts had scope bugs caught only by Claude Code's stop-and-report contract. That contract is working, but it's the last line of defense. Better prompts would walk the control flow explicitly before committing to unconditional-access patterns, and state structural outcomes in verification gates rather than literal counts. The stop-and-report pattern is load-bearing for this workflow; it should not be asked to compensate for specs that could have been structurally sound to begin with.

*Session-level postmortem worth preserving — session 4 (band-closing cadence):* The architectural doc describes the goal as rules clear → enforcement real → code, and implies this progression is strategic rather than tactical. Session 4 showed it can be a single-session shape when the principle is contained enough. The pattern that worked: state scan → draft ADR → empirical sweep against live code → refactor to compliance → author the enforcement rule → close with audit. Each step produced a verifiable artifact; each step's artifact informed the next step. The parked-list grew at every step, and the discipline was to name each new item without inspecting it and keep moving. That is the shape to internalize for future band-closing sessions — when a principle is small enough and the machinery mature enough, the full arc can close in one sitting.

*Session-level postmortem worth preserving — session 4 ("dead code" vs "dormant pipeline"):* The attribution sweep flagged `remediate_cognitive_role.py` as a violation; shallow read said "dead code, delete." The governor's push-back — *is the designed functionality needed by any stretch of the imagination?* — forced a second-pass read of the downstream architecture. The second-pass read surfaced the full Class B pipeline (CallSiteRewriter / PromptModel / Crate / Canary / apply). Deletion would have amputated a working architecture. The lesson is structural: "dead code" on first-pass analysis should prompt a second-pass read of the downstream architecture before proposing removal. An atomic action exists because someone wired an ingestion point; the ingestion point exists to feed something.

*Session-level postmortem worth preserving — session 4 (self-governance arrived):* The new attribution rule fired on its own session's documentation within minutes of being applied. The ERROR was self-inflicted — the function docstring explaining ADR-011 quoted its forbidden syntax — and the fix was a 6-line docstring reword. This is the closest CORE has come to *a governance rule catching a governance-text mistake on the same day the rule shipped*. The system is self-governing now on at least one principle. That is a milestone worth naming. It also generalises: any deterministic string-matching rule can catch its own documentation; the response is writing discipline, not rule complexity. Candidate for a tiny writing-convention ADR.

*Session-level postmortem worth preserving — session 4 (daemon-collision, cleaned up):* Local history was rewriteable because nothing had been pushed. If `211c2dd2` had been pushed, the cleanup would be political rather than technical — a governance-surface commit permanently titled as an autonomous format fix. Worth thinking about: pre-push review discipline for band-A sessions is not currently codified, and the daemon-collision is one of the parkable hazards that could benefit from an explicit policy. The cleanup pattern (`git reset --soft HEAD~1` → selective restage → two honest commits) worked cleanly and is worth keeping as institutional knowledge for future collisions.
