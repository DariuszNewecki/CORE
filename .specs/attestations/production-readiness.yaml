# Production-Readiness Attestation Manifest
# Home (governor-confirmed 2026-07-22): .specs/attestations/production-readiness.yaml
# Companion to .specs/attestations/proof-index.yaml in the attestation namespace.
#
# Single machine-readable record of the CURRENT verdict + evidence for the fifteen
# production gates. Gate DEFINITIONS (requirement + acceptance criteria) live in
# .specs/requirements/URS-production-readiness.md; this manifest records STATE and points
# back per gate. The README "Production Readiness" table is GENERATED from this file.
#
# BINARY VERDICT vs GRANULAR STATUS:
#   The authoritative verdict is binary (URS §4): every gate not `met` counts as NOT MET,
#   and until all fifteen are met the overall verdict is "Production readiness: NOT ATTESTED".
#   `status` below is PROGRESS granularity only (a communication aid), never a claim that a
#   non-`met` gate is close.
#
# INVARIANT (validator + CI drift-check enforced, see `integrity` below):
#   status in {met, mostly_met}  REQUIRES  non-empty evidence[]  AND an attestation with
#   verified_by + verified_at. A status is a PROOF, not a declaration — the same
#   anti-vacuous-pass discipline G9 requires of the audit itself. Claude never signs an
#   attestation; a human does.

schema_version: 1
definition_source: .specs/requirements/URS-production-readiness.md
generates: README.md#production-readiness
overall_verdict: NOT ATTESTED           # derived: not all 15 gates met

# Closed status vocabulary (weakest -> strongest):
#   not_started       — no mechanism, no work begun
#   not_demonstrated  — mechanism exists but the proving run has never run
#   partial           — some acceptance sub-criteria met, others open
#   not_met           — a MANDATORY acceptance criterion is actively violated by an open
#                       blocker; the verdict is not-met regardless of other progress
#                       (distinct from partial = incremental progress, no invariant break)
#   mostly_met        — all but a narrow named residual met  (needs attestation)
#   met               — every acceptance criterion satisfied  (needs attestation)

gates:
  - id: G1
    name: Fresh-install proof
    status: partial
    gap: CI-automated fresh-install absent; manual cold-room only
    evidence:
      - "Proxmox cold-room: 4 clean runs 2026-06-14 (manual, unpinned)"
    attestation: null
    blocking_issues: [796]              # install-core.sh CI is #796 scope (was #562 — closed)
    urs_ref: URS-production-readiness.md#g1

  - id: G2
    name: Constitutional enforcement is deterministic and regression-tested
    status: partial
    gap: Per-rule known-violating + known-compliant fixtures not universally applied
    evidence: []
    attestation: null
    blocking_issues: [842]               # corrected 2026-08-29 — was [773], but G2's
                                          # gap never appeared in #773's text; #842 is
                                          # the dedicated issue (blocker-link fix only,
                                          # gap/status unchanged)
    urs_ref: URS-production-readiness.md#g2

  - id: G3
    name: Layer integrity is machine-enforced and blocking
    status: partial
    gap: "Several layer rules at reporting severity (no_body_to_will, will.no_direct_database_access); reporting-only set not formally documented"
    evidence:
      - "ADR-140 closed the cognitive/write separation gap"
    attestation: null
    blocking_issues: [799]              # inferred — ramp-arc work, no dedicated issue
    urs_ref: URS-production-readiness.md#g3

  - id: G4
    name: Autonomous loop reliability demonstrated by soak
    status: not_demonstrated
    gap: >-
      A 72h soak ran (f7430b25, 2026-07-23 → 2026-07-26; NRestarts=0, ~35 workers,
      68,495 blackboard entries) and proved loop continuity only: 0 of 16,445 findings
      posted in the window reached a proposal or a consequence record — the only
      actionable class was abandoned by the remediation circuit breaker before any
      proposal could be created. Not accepted as G4 evidence. A qualifying soak needs a
      pre-declared throughput condition (≥ 1 finding → proposal → consequence chain
      completed under soak) recorded before it starts.
    evidence:
      - ".specs/attestations/soak-closure-f7430b25-20260726.md (continuity; Note 2026-09-14)"
      - ".specs/attestations/soak-f7430b25-findings-to-outcome-20260914.md (0/16,445; reproduced 2026-09-14)"
    attestation: null
    blocking_issues: [799]
    urs_ref: URS-production-readiness.md#g4

  - id: G5
    name: Mutation-lane equivalence and lifecycle safety
    status: partial                     # mandatory lane-equivalence criterion (#818) is closed;
                                        # two non-mandatory acceptance sub-criteria remain open.
    gap: "claim.proposal not yet confirmed as the sole claim-transition entry point by formal audit; approval_authority population on every approved proposal not yet verified"
    evidence:
      - "ADR-148 finalization barrier live (COMPLETED proves committed + durable consequence)"
      - "ADR-154 mutation-lane equivalence accepted"
      - "#818 closed 2026-08-24 (bd446c52/31cbfee4/dfa48512/65211a25): RemediationCeremony write=True structurally removed (D4); ADR-154 D1-D4 E2E-proven end-to-end against core_test, CI green (3911 passed)"
    history: "ADR-154 established lane equivalence as a mandatory criterion; #818 was the open bypass and is now closed. Residual gap is the two remaining unaudited acceptance sub-criteria, not the mandatory lane-equivalence invariant."
    attestation: null
    blocking_issues: [799]               # inferred — claim.proposal audit + approval_authority verification not separately tracked
    urs_ref: URS-production-readiness.md#g5

  - id: G6
    name: Risk model is governed, correct, and regression-tested
    status: partial                     # mandatory exhaustive-handling not independently
                                        # audited (own evidence admits the else-branch); not attested
    gap: "else-branch safety net exists for future StepKinds; known set handled but exhaustive-handling not independently audited"
    evidence:
      - "4c104dea: cognitive-step fallthrough fix + regression test"
    attestation: null
    blocking_issues: []
    urs_ref: URS-production-readiness.md#g6

  - id: G7
    name: Circuit breakers operate at the correct granularity
    status: partial                     # flow/worker-level breakers not yet audited; not attested
    gap: "Flow-level and worker-level circuit breakers not formally audited"
    evidence:
      - "4c104dea: per-symbol fix + test_test_remediator_symbol_query.py"
      - "per-file cap exists and is tested"
    attestation: null
    blocking_issues: []
    urs_ref: URS-production-readiness.md#g7

  - id: G8
    name: Integration tests for the governed mutation chain
    status: met                          # all 3 URS acceptance criteria proven by real,
                                          # unmocked integration tests including blackboard
                                          # evidence; governor-attested 2026-09-04
    gap: "None — no G8 acceptance criterion remains outstanding."
    evidence:
      - "CI provisions PostgreSQL + Qdrant; @pytest.mark.integration tests exist (infra present)"
      - "tests/will/autonomy/test_build_test_for_symbol_e2e_acceptance.py — three real-DB, real-sandbox integration tests for flow.build_test_for_symbol, extended this session to drive the proposal through the real orchestration Worker (ProposalConsumerWorker.run(), not a direct ProposalExecutor.execute() call) and assert real core.blackboard_entries evidence, per architecture.flows.flow_must_not_post_to_blackboard (the flow itself never posts; the Worker is the responsible layer, proven via Worker.post_report's proposal_consumer_worker.run.complete report, queried back from the real DB):"
      - "  - test_build_test_for_symbol_happy_path_reaches_completed_with_durable_consequence: real TestGenCognitiveDelegate generate/accept loop -> real write -> real sandboxed pytest validation -> real git commit -> durable core.proposal_consequences row -> real blackboard report with ok=True, lifecycle_status=completed for this proposal_id"
      - "  - test_build_test_for_symbol_no_output_fails_closed_with_no_write: stubbed LLM returns no code fence -> FlowExecutor halts before build.test_for_symbol runs -> no file written, HEAD unmoved, proposal failed -> real blackboard report with ok=False, lifecycle_status=failed"
      - "  - test_build_test_for_symbol_sandbox_validate_failure_produces_no_commit: accepted candidate, forced test.sandbox_validate failure -> ADR-106/107 hermetic-worktree write never propagates, no commit -> real blackboard report with ok=False, lifecycle_status=failed"
      - "Only PromptModel.invoke is stubbed (the one genuinely external, nondeterministic LLM call) — Worker orchestration, proposal lifecycle, FlowExecutor, the write action, sandbox validation, the database, the consequence chain, and blackboard posting are all real, unmocked components"
    attestation:
      verified_by: Dariusz Newecki
      verified_at: "2026-09-04T19:10:43Z"
    blocking_issues: []
    urs_ref: URS-production-readiness.md#g8

  - id: G9
    name: Enforcement integrity fails closed
    status: partial
    gap: "Skipped blocking rules not yet proven distinguishable-from-covered by CI fixture; empty-graph vacuous-pass guard not built (unmapped non-advisory PASS gap, #822, is closed)"
    evidence:
      - "623b745a / 4d0a316b: dispatch contracts — unsupported/undispatched check_type BLOCKs"
      - "#820 closed — Groups A, B, C complete: inert blocking rules eliminated; linkage.duplicate_ids now enforces statelessly via ast_gate (d4fd452a)"
      - "d4694a6a closed #822 (ADR-156): ConstitutionalAuditor._determine_verdict now DEGRADEs on any_unmapped_mapping_required_rules instead of silently returning PASS; 13 new/extended tests; verified against this repo's live .intent/ (343 policies, 256 rules)"
    attestation: null
    blocking_issues: [799]               # inferred — skipped-rule + vacuous-empty-state fixtures not separately tracked
    urs_ref: URS-production-readiness.md#g9

  - id: G10
    name: Operator observability
    status: partial
    gap: "Questions 4 (failure diagnosis) and 5 (rollback) not confirmed answerable by a non-author without source access"
    evidence:
      - "core-admin runtime dashboard, proposals show, consequence-chain query exist"
    attestation: null
    blocking_issues: [797]
    urs_ref: URS-production-readiness.md#g10

  - id: G11
    name: Upgrade and migration safety
    status: met                         # ADR-162 U0–U8 landed; Governor-attested 2026-09-19 with the 2.10.2 release decision
    gap: "None — every URS acceptance criterion is proven executable on disposable PostgreSQL for both released baselines (v2.9.1, v2.10.1); the route ships with 2.10.2. Governor ruling 2026-09-19: the declared updated_at changes during a v2.9.1 upgrade (touch triggers fired by the 20260722 backfill; #885 draft->pending) are acceptable — G11 requires preservation of governance history, not byte-identical storage."
    evidence:
      - "ADR-162 (accepted 2026-09-19) R1–R11 + amendments; units U0–U8a on main (0791258b … 912bee9d), U8b = this release"
      - "tests/shared/infrastructure/migrations/test_g11_release_candidate_postgres.py — v2.9.1->current and v2.10.1->current through the real CLI route (status -> --adopt-baseline <tag> --write -> migrate --write -> status) end CURRENT with 0 normalised schema-diff lines; seeded blackboard_entries / autonomous_proposals / proposal_consequences rows and their relationships preserved column-for-column (declared changes only: #885 draft->pending, 20260722 updated_at touch); a failing release migration is rolled back and unrecorded with earlier ones kept, retries exactly once after correction; 3 concurrent upgraders apply each migration once"
      - "tests/shared/infrastructure/migrations/test_hop_equivalence_postgres.py — every declared baseline + manifest replay == fresh schema.sql (ADR-162 D5, CI job)"
      - "tests/shared/infrastructure/migrations/test_ledger_engine_postgres.py — one transaction per migration under an advisory lock: a failed migration leaves no change and no ledger row; retry after fix; exactly-once under concurrency; verify-after-execute rollback (ADR-162 D7)"
      - "tests/shared/infrastructure/migrations/test_reconciliation_postgres.py + test_backfill_reconstruction_postgres.py — reconciliation only on a complete, catalog-structural postcondition (D12 §2, U5b); no DROP SCHEMA CASCADE anywhere in the route"
      - "tests/shared/infrastructure/migrations/test_wheel_migrate_postgres.py — installed wheel, no checkout, only DATABASE_URL: both baselines status -> adopt -> migrate -> CURRENT with bundled assets (D8)"
      - "tests/shared/infrastructure/migrations/test_schema_gate_postgres.py + tests/infra/test_core_engine_entrypoint.py — API, daemon and core-engine refuse a non-current schema and name the remedy (D2)"
      - "tests/shared/infrastructure/migrations/test_installer_schema_state_postgres.py + tests/infra/test_install_core_schema_gate.py — install-core.sh loads only a genuinely empty database (one transaction, no drop-and-retry), continues on current, refuses every other state before any service starts, never migrates (D10)"
      - "Release 2.10.2 — the first release carrying the route; operator procedure in docs/getting-started.md#upgrading-an-existing-core-database"
    attestation:
      verified_by: Dariusz Newecki
      verified_at: "2026-09-19T18:37:03Z"
    blocking_issues: []
    urs_ref: URS-production-readiness.md#g11

  - id: G12
    name: Runtime trust boundary is audited and documented
    status: partial
    gap: "Rate-limiting posture not fully documented; user-facing route safety not fully confirmed per-route"
    evidence:
      - "SECURITY.md present — documents runtime/platform trust boundary (runtime API unauthenticated by design; auth extracted to core-platform)"
      - "#671 closed — exposure tiers on routes (ADR-110 D5)"
      - "ADR-132 governor auth (now a core-platform concern)"
    attestation: null
    blocking_issues: [796]
    urs_ref: URS-production-readiness.md#g12

  - id: G13
    name: Documentation for operators
    status: partial
    gap: "No operator runbook; CLAUDE.md is comprehensive for Claude Code but is not an operator document"
    evidence:
      - "install-core.sh and scripts/demo.sh are self-documenting"
    attestation: null
    blocking_issues: [797]
    urs_ref: URS-production-readiness.md#g13

  - id: G14
    name: Source reversibility
    status: partial
    gap: "Source-restoration-after-completed-mutation not proven by test; rollback-vs-forward audit distinction not verified"
    evidence:
      - "ADR-148 rollback_proposal restores pre-execution state on FAILED/REFUSED"
      - "SandboxLifecycle checkpoint/restore of full write footprint (#815/#816/#817)"
    attestation: null
    blocking_issues: [799]              # inferred — no dedicated reversibility-proof issue yet
    urs_ref: URS-production-readiness.md#g14

  - id: G15
    name: Release integrity
    status: partial
    gap: "Release-gating checks not fully enforced as a blocking pre-publish gate; release provenance (tag/SHA/check-run) not queryable"
    evidence:
      - "publish-pypi.yml verifies tag == pyproject version, OIDC, no stored token"
      - "core-ci.yml runs --offline --severity=block per push"
    attestation: null
    blocking_issues: [796]              # inferred — CI/release hygiene is #796 scope
    urs_ref: URS-production-readiness.md#g15

# Derived rollup (computed by the generator, NOT hand-written):
#   met 0/15 · mostly_met 0 · partial 13 ·
#   not_demonstrated 1 (G4) · not_met 1 (G11)
#   -> overall_verdict: NOT ATTESTED. No composite score.

integrity:
  # Validator + CI drift-check (mirrors the existing README rule-count drift guard):
  #   1. status met|mostly_met without evidence[] + attestation   -> FAIL
  #   2. attestation.verified_by is an AI/Claude handle            -> FAIL
  #   3. rendered README table != this manifest                   -> FAIL
  #   4. attestation.verified_at older than newest evidence sha    -> STALE (warn -> block)
  #   5. overall_verdict != derived-from-gates                     -> FAIL
  home: scripts/ generator + CI step (per governor decision 2026-07-22)
