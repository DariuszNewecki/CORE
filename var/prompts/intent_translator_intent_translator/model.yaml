id: intent_translator_intent_translator
version: "0.1.0"
role: IntentTranslator
description: Translates raw user input into a structured goal specification for the PlannerAgent.
input:
  required:
    - user_input
output:
  format: json
success_criteria: Returns a valid JSON object containing a structured goal with clear intent, constraints, and context extracted from user input.
