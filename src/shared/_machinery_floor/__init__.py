# src/shared/_machinery_floor/__init__.py
# Package marker — allows importlib.resources.files("shared._machinery_floor") to
# resolve the bundled machinery floor data as a Python package resource (ADR-108 D3).
