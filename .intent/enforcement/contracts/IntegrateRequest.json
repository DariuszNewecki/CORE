{
    "$schema": "META/data_contract.schema.json",
    "kind": "data_contract",
    "metadata": {
        "id": "contracts.integrate_request",
        "title": "IntegrateRequest \u2014 body of POST /v1/integration/commit",
        "version": "1.0.0",
        "authority": "constitution",
        "status": "active"
    },
    "description": "Request body to trigger a governed commit of an approved proposal's diff (POST /v1/integration/commit, integration_routes.py). Crosses the API boundary; constitutionally required to carry a governing schema (ADR-056 D7). 'commit_message' is required.",
    "governed_classes": [
        "IntegrateRequest"
    ],
    "required": [
        "commit_message"
    ],
    "properties": {
        "commit_message": {
            "type": "string",
            "description": "Commit message to attach to the governed integration commit."
        }
    }
}
