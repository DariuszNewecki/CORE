{
  "description": "Defines the canonical file format requirements for constitutional and policy artifacts in .intent/, ensuring deterministic parsing, schema validation, and long-term machine governance.",
  "id": "standard_operations_constitution_format",
  "rules": [
    {
      "id": "constitution.primary_format",
      "rationale": "CORE treats .intent/ as a machine-oriented source of truth. JSON provides deterministic parsing, strong schema validation, broad tooling support, and unambiguous structure, which is required for automated governance and reliable UI rendering.",
      "enforcement": "error",
      "statement": "The canonical format for constitutional content in .intent/ must be JSON.",
      "check": {
        "engine": "llm_gate",
        "params": {
          "instruction": "Verify adherence to: The canonical format for constitutional content in .intent/ must be JSON."
        }
      }
    },
    {
      "id": "constitution.allow_legacy_yaml",
      "notes": "Target full JSON adoption by end of 2026.",
      "rationale": "A hard cutover can block delivery and increase operational risk. A staged migration preserves velocity while converging on JSON as the single canonical representation.",
      "enforcement": "warning",
      "statement": "Legacy YAML intent artifacts may be tolerated during a controlled migration period, but are not canonical.",
      "check": {
        "engine": "llm_gate",
        "params": {
          "instruction": "Verify adherence to: Legacy YAML intent artifacts may be tolerated during a controlled migration period, but are not canonical."
        }
      }
    }
  ],
  "schema_id": "meta.policy",
  "title": "Constitution Format Standard",
  "version": "1.0.0",
  "owners": {
    "accountable": "Governance Lead"
  },
  "review": {
    "frequency": "12 months"
  },
  "type": "policy",
  "additionalProperties": false
}
