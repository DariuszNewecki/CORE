# .intent/charter/schemas/operations/config_schema.yaml
id: schema_operations_config
version: "1.0.0"
title: "Operations Configuration Schema"
type: "schema_operations_config"
status: active

owners:
  accountable: "Platform SRE"
  responsible:
    - "Core Maintainer"

review:
  frequency: "12 months"

schema_id: "schema.operations.config"

git:
  ignore_validation:
    type: boolean
    default: false
    description: >
      If true, skips Git pre-write checks. MUST be false in production or fallback modes
      to maintain rollback safety. Only for emergency recovery.
