{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "schema_id": "meta.constitution",
  "version": "1.1.0",
  "title": "CORE Constitution Schema",
  "description": "Validates core authority and boundary documents, allowing principles to act as enforceable rules.",
  "type": "object",
  "additionalProperties": true,
  "required": [
    "id",
    "type",
    "principles",
    "domain"
  ],
  "properties": {
    "type": {
      "type": "string",
      "description": "Must match the constitutional domain kind (e.g., constitutional_authority)."
    },
    "domain": {
      "type": "string",
      "enum": [
        "authority",
        "boundaries",
        "risk",
        "precedence"
      ],
      "description": "The specific governance area."
    },
    "principles": {
      "type": "object",
      "description": "Dictionary of principles. Each principle follows the Rule schema to enable dynamic enforcement.",
      "patternProperties": {
        "^[a-z][a-z0-9_]*$": {
          "$ref": "rule.schema.json"
        }
      },
      "additionalProperties": false
    }
  }
}
