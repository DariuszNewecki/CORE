"""Example module inside the ratified ``package/`` prefix."""

from __future__ import annotations


def greet(name: str) -> str:
    """Return a friendly greeting for name."""
    return f"Hello, {name}!"
