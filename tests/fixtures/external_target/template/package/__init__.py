"""Neutral fixture package under the ratified safe-auto-approval prefix.

No concepts derived from any external project — this is a plain, generic
Python package that exists only to give the external-target authority
tests a real ``package/`` root to reason about.
"""
